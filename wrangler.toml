
name = "bevuihoctoan"
compatibility_date = "2025-11-25"

[site]
bucket = "."
```

5. Commit: "Add wrangler config"

### **Bước 2: Đợi Cloudflare auto-deploy**

Cloudflare sẽ tự động phát hiện thay đổi và deploy lại. Chờ 1-2 phút!

---

## ✅ **Cách 2: Thay đổi cấu hình trên Cloudflare (Nhanh hơn)**

### **Bước 1: Vào Settings của Project**

1. Vào Cloudflare Dashboard
2. Click vào project "bevuihoctoan" (hoặc tên bạn đặt)
3. Chọn tab **"Settings"**
4. Kéo xuống phần **"Build & deployments"**

### **Bước 2: Sửa Build Configuration**

Tìm phần **"Build configuration"** và sửa như sau:

- **Framework preset**: `None`
- **Build command**: *(xóa hết, để trống)*
- **Build output directory**: `/` (hoặc `./`)
- **Root directory**: `/` (hoặc để trống)

### **Bước 3: Save và Retry Deployment**

1. Click **"Save"**
2. Quay lại tab **"Deployments"**
3. Tìm deployment bị failed
4. Click nút **"Retry deployment"** (3 chấm bên phải → Retry)

---

## ✅ **Cách 3: Tạo lại Project từ đầu (Nếu 2 cách trên không work)**

1. **Xóa project hiện tại** trên Cloudflare
2. **Tạo lại project mới**:
   - Vào **"Workers & Pages"**
   - **"Create application"** → **"Pages"**
   - **"Upload assets"** (KHÔNG chọn "Connect to Git")
   - Kéo thả file `index.html` trực tiếp vào
   - Click **"Deploy site"**

Cách này đơn giản nhất nhưng mỗi lần cập nhật phải upload thủ công.

---

## 🎯 **Khuyến nghị:**

**Dùng Cách 1** (thêm `wrangler.toml`) vì:
- Tự động deploy mỗi khi push code
- Chuyên nghiệp hơn
- Dễ quản lý

---

## 📋 **Cấu trúc file cuối cùng trên GitHub:**
```
hocvui/
├── index.html       ✅ (đã có)
└── wrangler.toml    ⚠️ (cần thêm)
